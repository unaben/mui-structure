import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1760a5",
      light: "steelblue",
    },
    secondary: {
      main: "#d5bdaf",
      light: "seagreen",
    },
    success: {
      main: "#3a5a40",
      light: "coral",
    },
  },
});